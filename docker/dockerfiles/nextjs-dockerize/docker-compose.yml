version: '3.8'

services:
  nextjs:
    image: ghcr.io/YOUR_GITHUB_USERNAME/YOUR_REPO_NAME:latest
    container_name: nextjs-app
    restart: always
    ports:
      - "3000:3000"
    env_file:
      - .env.production
